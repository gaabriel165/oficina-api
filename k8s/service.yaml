apiVersion: v1
kind: Service
metadata:
  name: oficina-api
  labels:
    app: oficina-api
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: oficina-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
