apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oficina-api-homolog

resources:
  - namespace.yaml
  - ../../base

replicas:
  - name: oficina-api
    count: 1

patches:
  - path: configmap-patch.yaml
  - path: hpa-patch.yaml
  - path: service-patch.yaml
